# autocodebook 0.3.12

* Report only shows the "Eligibility" section when tracking steps exist
  (supports pure data-processing datasets such as the SPEI base).

# autocodebook 0.3.0

* Big-data optimizations: `cache`, `assume_unique`, `cb_checkpoint()`.
* Standardized report module: eligibility flowchart, editable codebook
  export (HTML/DOCX/XLSX), cross-sectional / longitudinal inspection.
* CONSORT-style flow tree: `track_split()`, `track_outcomes()`, `flow_table()`.

# autocodebook 0.1.0

* Initial API: `auto_mutate()`, `auto_summarise()`, `auto_filter()`,
  codebook registration and eligibility tracking.
