% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_verbs.R
\name{auto_filter}
\alias{auto_filter}
\title{Filter with automatic tracking}
\usage{
auto_filter(
  .data,
  step = "",
  description = "",
  ...,
  cache = NULL,
  assume_unique = FALSE
)
}
\arguments{
\item{.data}{A Spark DataFrame or local data frame.}

\item{step}{Character label for this filtering step.}

\item{description}{Character description of the filter.}

\item{...}{Filter conditions, same syntax as \code{dplyr::filter()}.}

\item{cache}{Logical or NULL (named-only). If TRUE, materializes the
result with \code{cb_checkpoint()} after filtering - useful in long Spark
pipelines. If NULL, falls back to the session default (set via
\code{cb_init()} or \code{cb_set_default_cache()}). Default: NULL.}

\item{assume_unique}{Logical (named-only). Passed to \code{track_step()}.
Set TRUE only when you are certain the ID column has no duplicates
at this stage. Default: FALSE.}
}
\value{
The filtered data frame.
}
\description{
Works exactly like \code{\link[dplyr:filter]{dplyr::filter()}}, but also logs a tracking step
recording how many unique IDs remain after the filter.
}
\details{
The signature mirrors v0.1.0 for full backward compatibility: \code{step}
and \code{description} come first (so existing positional calls keep working),
then \code{...} for the filter conditions, and finally the new big-data
options (\code{cache}, \code{assume_unique}) which \strong{must be passed by name}.
}
