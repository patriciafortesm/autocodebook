% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_tracking.R
\name{cb_checkpoint}
\alias{cb_checkpoint}
\title{Checkpoint a Spark DataFrame}
\usage{
cb_checkpoint(sdf, name = NULL, mode = c("memory", "disk", "register"))
}
\arguments{
\item{sdf}{A Spark DataFrame (tbl_spark) or local data frame.}

\item{name}{Optional. Name to register the checkpoint under (Spark only).
If NULL, a temporary name is generated.}

\item{mode}{Character. One of \code{"memory"} (cache in memory, fastest),
\code{"disk"} (sdf_checkpoint via disk, more durable), or \code{"register"}
(just register as temp table without caching). Default: \code{"memory"}.}
}
\value{
The (possibly materialized) data frame.
}
\description{
Forces materialization of a lazy Spark plan. Useful in long pipelines where
query plans get too deep and the optimizer starts re-computing upstream
steps. For local data frames, this is a no-op.
}
