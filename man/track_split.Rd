% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_flow.R
\name{track_split}
\alias{track_split}
\title{Split the cohort into branches by a column (CONSORT flowchart)}
\usage{
track_split(sdf, by, label = NULL, value_labels = NULL, max_levels = 3L)
}
\arguments{
\item{sdf}{A Spark DataFrame or local data frame.}

\item{by}{Character. Column name to split by. Its distinct values become
the branches. NA values are grouped as "(NA)".}

\item{label}{Optional character. A human-readable name for this split
level (e.g. "Exposure: drought"). Defaults to \code{by}.}

\item{value_labels}{Optional named character vector mapping raw values to
readable labels, e.g. \code{c("0" = "Sem seca", "1" = "Com seca")}. If not
given, the function tries factor levels / labelled attributes on the
column; failing that, uses the raw value.}

\item{max_levels}{Integer. Safety cap on nesting depth. Default 3.}
}
\value{
\code{sdf} unchanged (for piping).
}
\description{
Adds one branching level to the flow tree. The cohort is divided by the
distinct values of \code{by}. Chain multiple \code{track_split()} calls to create
nested branches (e.g. exposure then mediator). Passes the data through
unchanged, so it fits in a \verb{\%>\%} pipeline.
}
\examples{
\dontrun{
df \%>\%
  track_split(by = "exposto_seca", label = "Exposição: seca",
              value_labels = c("0" = "Sem seca", "1" = "Com seca")) \%>\%
  track_split(by = "migrou", label = "Mediador: migração",
              value_labels = c("0" = "Não migrou", "1" = "Migrou")) \%>\%
  track_outcomes(c("obito_dcv", "obito_infec"))
}
}
